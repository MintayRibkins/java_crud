# TODO

Items below are based on the original task requirements.

- [ ] Complete full user update in `PUT /api/update_user` for all fields (`name`, `surname`, `email`, `address`).
- [ ] Improve CSV import validation in `POST /api/import_users_csv` (malformed line handling and clearer error reporting).
- [ ] Add Docker support (if still required): app `Dockerfile` + database in Docker (`docker-compose.yml`).
